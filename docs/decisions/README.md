# Decisions

Use decision records for durable choices that future agents should inherit.

Record a decision when:

- the stack changes
- a route or content architecture becomes stable
- validation expectations change
- a tradeoff is accepted
- a rejected alternative is likely to come up again

Do not record temporary debugging notes here. Use agentmemory only after a decision is stable.

